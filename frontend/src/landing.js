/* Entry for the static landing page — pulls the Tailwind stylesheet through
   Vite's pipeline. The landing page itself is plain HTML so crawlers see the
   full content without executing JavaScript. */
import "./index.css";
